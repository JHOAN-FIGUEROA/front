import { Typography, Box } from '@mui/material';

const productos = () => (
  <Box p={3}>
    <Typography variant="h5">Módulo de Productos</Typography>
    <Typography variant="body1" color="text.secondary">Aquí irá el panel de Productos.</Typography>
  </Box>
);

export default productos; 
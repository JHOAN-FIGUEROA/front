import { Typography, Box } from '@mui/material';

const categorias = () => (
  <Box p={3}>
    <Typography variant="h5">Módulo de Categorias</Typography>
    <Typography variant="body1" color="text.secondary">Aquí irá el panel de Categorias.</Typography>
  </Box>
);

export default categorias; 
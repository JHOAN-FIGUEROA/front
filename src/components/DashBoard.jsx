import { Typography, Box } from '@mui/material';

const DashBoard = () => (
  <Box p={3}>
    <Typography variant="h5">Módulo de DashBoard</Typography>
    <Typography variant="body1" color="text.secondary">Aquí irá el panel principal del sistema.</Typography>
  </Box>
);

export default DashBoard; 